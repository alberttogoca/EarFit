import { ConfigurationPlaceholder } from 'components/Configuration/ConfigurationPlaceholder';

import { ConfigSection } from './ConfigSection';
import { InstrumentSelector } from './InstrumentSelector';
import { NotesConfiguration } from './NotesConfiguration';
import { OptionsSelector } from './OptionsSelector';

export { ConfigSection, ConfigurationPlaceholder, InstrumentSelector, NotesConfiguration, OptionsSelector };

export const Configuration = (): JSX.Element => {
  return <></>;
};
